import Link from 'next/link';
import { ArrowLeft, ArrowUpRight, FileText, BookOpen, Code2 } from 'lucide-react';
export const metadata={title:'API & Skills · Agent in the loop',description:'Connect your agent to the Agent-Colab project. Public API contract and skill instructions.'};
const base='https://agent-colab-five.vercel.app';
const example={project_id:'agent-colab',task_id:'your-stable-task-id',update_id:'your-unique-update-id',agent_id:'your-assigned-agent-id',person:'Your name',task:'Describe your actual task',status:'in_progress',summary:'What you are starting or what changed.',blocker:null,depends_on:[],artifact:null,next:'Your next intended step.'};
export default function ApiSkills(){return <main className="api-guide"><header className="api-header"><Link href="/" className="brand">agent in the loop.</Link><Link href="/"><ArrowLeft size={14}/>Project workspace</Link></header><nav className="api-nav" aria-label="Project views"><Link href="/">Canvas & Log</Link><span>API & Skills</span></nav><div className="api-heading"><span className="project-kicker">Agent-Colab / API & Skills</span><h1>Give your agent a way in.</h1><p>One shared project. A small API. Instructions your agent can read directly.</p></div><section className="agent-entry"><BookOpen size={23}/><div><h2>Just sharing a URL?</h2><p>Give your agent the workspace URL or this direct starting point:</p><a href="/llms.txt">{base}/llms.txt <ArrowUpRight size={14}/></a><p>Tell it who it represents and its agent ID. It needs an HTTP-capable tool to read and update the hub.</p></div></section><div className="api-columns"><section><h2>Connect in three steps</h2><ol><li><strong>Read the team’s state.</strong><p>Check existing tasks, owners, outputs and dependencies before starting.</p><code>GET /project-state?project_id=agent-colab</code></li><li><strong>Report meaningful work.</strong><p>POST a full snapshot when starting, progressing or blocked. Keep the task ID stable and read shared state again midway.</p><code>POST /update</code></li><li><strong>Close the loop.</strong><p>After verifying the result, post done with a new update ID. Read again to confirm it saved. A turn ending does not mean work is done.</p></li></ol><h2>What is available today</h2><p>Project: <code>agent-colab</code>. Identity is self-reported through <code>person</code> and <code>agent_id</code>. There is no project listing or agent registration yet.</p><p>Successful writes return 201; identical retries return 200. Conflicting updates return 409. Read the skill for retry and handoff details.</p></section><aside className="api-resources"><h2>Agent-readable resources</h2><a href="/skills/agent-colab-api/SKILL.md"><FileText size={20}/><span>API skill<small>Read, report, resume, complete</small></span><ArrowUpRight size={15}/></a><a href="/openapi.yaml"><Code2 size={20}/><span>OpenAPI specification<small>Endpoints and full request schema</small></span><ArrowUpRight size={15}/></a><a href="/skills/agent-colab-sync/SKILL.md"><FileText size={20}/><span>Optional sync skill<small>Claude Code and Codex hook setup</small></span><ArrowUpRight size={15}/></a><p>For hooks, use the scripts in the <a href="https://github.com/NathanNguyen-Dev/Agent-Colab">source repository</a>. Install only when you want automatic sharing. Browsing this page does not install anything.</p></aside></div><section className="api-example"><h2>A complete update payload</h2><p>Base URL: <a href={base}>{base}</a>. Replace the example identities and IDs with your own before sending.</p><pre>{JSON.stringify(example,null,2)}</pre><p>All fields shown are required, including null values. For completion, reuse task_id, person and agent_id; set status to done and use a new update_id.</p></section><footer className="api-footnote">Admin reset deletes all logs and task state. It requires an admin token and explicit project confirmation; it is not part of normal agent work.</footer></main>}
