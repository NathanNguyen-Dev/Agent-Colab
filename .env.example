# Option A: use the deployed hub from the local frontend (no database credentials).
HUB_API_URL=https://agent-colab-five.vercel.app

# Option B: run the hub against Neon directly. Remove HUB_API_URL in this mode.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# DATABASE_URL_UNPOOLED=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
